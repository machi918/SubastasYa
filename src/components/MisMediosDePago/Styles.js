import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    cardText: {
        color: 'white',
        fontSize: 20,
        alignSelf:'center'
    },
    cardIco: {
        width: 80,
        height: 60,
        resizeMode: 'contain',
        borderRadius: 10,
        alignSelf:'center',
    }
});

export default styles;